V03 - the development-only Git harness is still absent from the production bundle
Date: 2026-09-16T20:52:55Z
Command: npm run tauri build -- --target x86_64-pc-windows-msvc (clean dist)

Milestone 7 added a shipped source-control panel beside the harness. The harness
itself is unchanged and must still be excluded, so the Milestone 6 check is rerun
against the new bundle.

$ grep -rl 'tbce-git-harness-dev-only' dist/
(no matches - the harness marker is not in the bundle)

$ grep -rl 'GitHarness' dist/
(no matches - the component is not in the bundle)

$ ls dist/assets | grep -i 'harness'
(no harness chunk was emitted)

The new diff preview does ship, as its own lazy chunk, which is expected:
$ ls dist/assets | grep -i 'DiffView'
DiffView-4yR_e2xR.js            1806 bytes

The shipped typed adapter src/services/git.ts still names the commands, as before:
$ grep -rl 'git_commit' dist/
dist/assets/index-B7guWeW6.js

The source-control panel is part of the main entry chunk rather than a lazy one,
because the activity bar renders it directly:
$ grep -c 'SOURCE CONTROL' dist/assets/index-B7guWeW6.js
1
